apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
    color: blue   # <-- This is the switch! Currently pointing to Blue.
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
